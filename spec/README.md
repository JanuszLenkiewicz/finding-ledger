# spec/ — finding-ledger specifications

Feature specs written BEFORE implementation (the spec and the implemented
state must agree). One feature = one file `spec/<feature-name>.md` with a
status line (draft / accepted / implemented — with dates).

Shipped since this note was written (v0.2.0, so no spec was ever needed):
promptfoo adapter, Langfuse scores adapter — see
[`docs/adapters.md`](../docs/adapters.md).

Remaining candidates: pattern registry (meta-counters with a graduation
threshold), judge calibration against a human-labelled sample.
